package init;

import model.arbol;

public class Completa extends initMethod{
	
	@Override
	public void inicia(arbol tree, int prof_min, int prof_max) {
		if(prof_min > 0) { //No puede ser hoja
			Tipo operador = 0;
		}
		
	}

}
